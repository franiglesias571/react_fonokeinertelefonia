import React from 'react';

export const Title = (props) => {
    
    return (

        <h1>{props.greeting}</h1> //"Tienes x cantidad de ítems en tu carrito mensaje, desde itemlistcontainer.jsx de carpeta itemlistcontainer"
    );
}
export default Title; 